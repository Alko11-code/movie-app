node_modules/
.env
.DS_Store
*.log
npm-debug.log*
.vscode/
dist/
build/
```

Then **Save the file** (Ctrl+S)

---

## **What Each Line Does:**

| Line | What It Ignores | Why? |
|------|-----------------|------|
| `node_modules/` | All installed packages | Too large, can reinstall with `npm install` |
| `.env` | Environment variables file | Contains secrets (passwords, API keys) |
| `.DS_Store` | Mac system file | Not needed in repository |
| `*.log` | All log files | Temporary files, not needed |
| `npm-debug.log*` | NPM error logs | Debugging info, not needed |
| `.vscode/` | VS Code settings | Personal preferences, not shared |
| `dist/` | Build folder | Generated files |
| `build/` | Build folder | Generated files |

---

## ✅ **Your `.gitignore` file should look exactly like this:**
```
node_modules/
.env
.DS_Store
*.log
npm-debug.log*
.vscode/
dist/
build/